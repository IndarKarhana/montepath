{
  "schema_version": "competitor-env.v1",
  "library": "cupy",
  "purpose": "CuPy CUDA accelerator baseline where CUDA hardware is available.",
  "hardware": "NVIDIA CUDA GPU",
  "install": [
    "python -m pip install -r benchmarks/competitors/requirements-accelerators.txt"
  ],
  "benchmark_script": "benchmarks/competitors/python_cpu_baselines.py",
  "lanes": [
    "terminal_distribution_gpu_cupy"
  ],
  "reproducibility": "Statistical reproducibility for same seed and CuPy/CUDA stack; exact replay is not guaranteed across devices or versions.",
  "telemetry": [
    "warmup_ms",
    "compile_ms",
    "execution_ms",
    "memory_peak_mb",
    "device"
  ]
}
