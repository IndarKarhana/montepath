{
  "schema_version": "competitor-env.v1",
  "library": "numba",
  "purpose": "CPU JIT Monte Carlo baseline.",
  "hardware": "CPU",
  "install": [
    "python -m pip install numba>=0.58 numpy>=1.24"
  ],
  "benchmark_script": "benchmarks/competitors/python_cpu_baselines.py",
  "lanes": [
    "stepwise_paths",
    "terminal_distribution"
  ],
  "reproducibility": "Seeded NumPy RNG inside Numba JIT; execution timing excludes warm-up compile."
}
