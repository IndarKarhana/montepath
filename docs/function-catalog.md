# Function Catalog

This document is the project-local index of important public functions and agent-callable surfaces.

## Purpose

- help contributors and agents find the right entry point quickly
- make public behavior explicit without forcing full codebase exploration
- define which surfaces are ready for future tool wrapping

## Update Rule

Update this file whenever a public function or public-facing helper is added or materially changed.

Minimum information per entry:

- path
- function or surface name
- purpose
- key inputs and outputs
- determinism or reproducibility notes
- caveats
- agent-tool readiness

## Current Public Surface

### `mc-schema`

| Surface | Path | Purpose | Inputs / Outputs | Determinism | Caveats | Agent-tool readiness |
| --- | --- | --- | --- | --- | --- | --- |
| `SimulationSpecBuilder` | `crates/mc-schema/src/builder.rs` | Ergonomic builder for `SimulationSpec` construction. | Inputs: chained builder calls. Output: `SimulationSpec`. | Deterministic. | Builder convenience layer, not execution logic. | Good candidate for future language bindings, not yet a standalone agent tool. |
| `validate_simulation_spec` | `crates/mc-schema/src/validate.rs` | Validate a simulation definition and emit structured diagnostics. | Input: `&SimulationSpec`. Output: `Vec<Diagnostic>`. | Deterministic. | Current coverage is schema-level, not full execution feasibility. | Strong candidate for an agent validation tool. |
| `check_schema_compatibility` | `crates/mc-schema/src/compat.rs` | Check schema version support and compatibility expectations. | Input: schema version metadata. Output: `CompatibilityReport`. | Deterministic. | Focused on version compatibility, not semantic correctness. | Good candidate for an agent compatibility tool. |

### `mc-core` planner

| Surface | Path | Purpose | Inputs / Outputs | Determinism | Caveats | Agent-tool readiness |
| --- | --- | --- | --- | --- | --- | --- |
| `normalize_run_config` | `crates/mc-core/src/planner/mod.rs` | Normalize and validate runtime execution settings. | Input: `RunConfig`. Output: `Result<NormalizedRunConfig, PlannerError>`. | Deterministic. | Only validates current planner config rules. | Useful helper, but better wrapped inside higher-level planning tools. |
| `extract_features` | `crates/mc-core/src/planner/mod.rs` | Compute structural features from a `SimulationSpec`. | Input: `&SimulationSpec`. Output: `FeatureSummary`. | Deterministic. | Feature set is currently v1-focused. | Good candidate for an agent analysis tool. |
| `plan_execution` | `crates/mc-core/src/planner/mod.rs` | Choose backend and produce `ExecutionPlan` with explainability. | Inputs: `&SimulationSpec`, `RunConfig`, backend support reports. Output: `Result<ExecutionPlan, PlannerError>`. | Deterministic for same inputs and support reports. | Current heuristics are still evolving, especially for GPU paths and broader workload breadth. | High-value candidate for an agent planning tool. |
| `explain_execution_plan` | `crates/mc-core/src/planner/mod.rs` | Render a compact textual explanation of an `ExecutionPlan`. | Input: `&ExecutionPlan`. Output: `String`. | Deterministic for the same plan. | Human-readable helper layered on top of structured planner output. | Good companion surface for agent and user explanations. |
| `recommend_method` | `crates/mc-core/src/planner/mod.rs` | Recommend a Monte Carlo method, sampling choice, and variance-reduction technique for a workload shape. | Input: `MethodRecommendationRequest`. Output: `MethodRecommendation` with `method_id`, sampling, technique, reasons, and caveats. | Deterministic. | Heuristic policy backed by current benchmark evidence, including European realized-error evidence against Black-Scholes; MLMC recommendations currently point to the arithmetic-Asian CPU reference surface. | Strong agent/tool planning surface. |

### `mc-core` backend and runtime

| Surface | Path | Purpose | Inputs / Outputs | Determinism | Caveats | Agent-tool readiness |
| --- | --- | --- | --- | --- | --- | --- |
| `builtin_backends` | `crates/mc-core/src/backend/mod.rs` | Build the built-in backend registry. | No inputs. Output: `Vec<Box<dyn RuntimeBackend>>`. | Deterministic aside from runtime device discovery results. | Discovery depends on host environment, and current GPU backends may execute through delegated CPU fallback until native kernels land. | Internal support surface, usually not a direct tool endpoint. |
| `plan_gpu_chunking` | `crates/mc-core/src/backend/mod.rs` | Estimate chunking strategy for GPU execution under memory budgets. | Inputs: total paths, device memory, `GpuChunkingConfig`. Output: `GpuChunkingPlan`. | Deterministic. | Planning heuristic only; not proof of runtime support. | Strong future tool candidate for backend explainability. |
| `estimate_gpu_bytes_per_path` | `crates/mc-core/src/backend/mod.rs` | Conservative per-path memory estimate for GPU cost modeling. | Input: `&ExecutionPlan`. Output: `usize`. | Deterministic. | Heuristic estimate, not measured runtime telemetry. | Internal helper unless exposed with explanation context. |
| `cuda_native_feature_enabled` | `crates/mc-core/src/backend/cuda.rs` | Report whether host-side native CUDA staging is compiled in. | No inputs. Output: `bool`. | Deterministic per build. | Does not imply CUDA hardware or native execution availability. | Useful agent/tool diagnostic surface. |
| `metal_native_feature_enabled` | `crates/mc-core/src/backend/metal.rs` | Report whether host-side native Metal staging is compiled in. | No inputs. Output: `bool`. | Deterministic per build. | Does not imply Apple GPU hardware or native execution availability. | Useful agent/tool diagnostic surface. |
| `NativeArtifactMetadata` | `crates/mc-core/src/backend/mod.rs` | Describe staged native kernel artifacts for GPU backends. | Output fields include kernel family, entry point, source module, compile request status, compile success status, optional compiled module path, and kernel contract metadata. | Deterministic for same build and environment. | Compile success still depends on toolchain availability; native execution breadth still varies by backend. | Strong future agent/tool diagnostic surface. |
| `GpuKernelContract` | `crates/mc-core/src/backend/mod.rs` | Describe the staged GPU launch ABI for a kernel family. | Output fields include buffer bindings, scalar bindings, and launch dimensions. | Deterministic for same plan. | Represents staged ABI, not proof of native launch success. | Strong future agent/tool diagnostic and explainability surface. |
| `BackendExecutionInput` | `crates/mc-core/src/backend/mod.rs` | Typed execution input enum for backend dispatch. | Variants include `EuropeanCall`, `ArithmeticAsianCall`, and `DownAndOutCall`. | Deterministic. | GPU native support differs by backend and sampling choice. | Strong future tool/runtime surface once exposed more directly. |
| `AppleMetalBackend::execute` | `crates/mc-core/src/backend/metal.rs` | Execute the staged v1 Metal kernel path on macOS when `metal-native` is enabled, using cached in-process Metal pipelines when native support is available and falling back otherwise. | Inputs: `CompiledArtifact`, `BackendExecutionInput::{EuropeanCall, ArithmeticAsianCall, DownAndOutCall}`. Output: `RunOutput`. | Deterministic for identical config and seed. | Native path currently supports pseudorandom sampling and the current GBM option family; randomized-QMC and Latin-hypercube requests still fall back to CPU reference execution. | Strong future tool/runtime surface, with a real native Apple GPU execution path. |
| `SamplingMethod` | `crates/mc-core/src/runtime/cpu.rs` | Select the sampling strategy independently from variance-reduction technique. | Input enum. Output: controls runtime behavior. | Deterministic. | Current support is `Pseudorandom`, `RandomizedHalton`, `LatinHypercube`, `ScrambledSobol`, and `ScrambledSobolBrownianBridge`; native GPU structured sampling is not implemented yet. | Strong future tool parameter surface. |
| `MonteCarloTechnique` | `crates/mc-core/src/runtime/cpu.rs` | Select variance-reduction technique for current runtimes. | Input enum. Output: controls runtime behavior. | Deterministic. | Current support is `Standard`, `Antithetic`, and workload-specific `ControlVariate`. | Good future tool parameter surface. |
| `monte_carlo_method_capabilities` | `crates/mc-core/src/runtime/cpu.rs` | Return a machine-readable catalog of supported and planned Monte Carlo methods by backend. | No inputs. Output: `Vec<MonteCarloMethodCapability>`, including method id, category, CPU/Metal/CUDA support states, and notes. | Deterministic. | It is a capability catalog, not runtime telemetry; CUDA native execution remains planned. | Strong agent/tool discovery surface. |
| `generate_standard_normals_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Generate deterministic standard-normal samples for pseudorandom and structured sampling methods. | Inputs: `SamplingMethod`, point count, dimension count, and seed. Output: row-major `Vec<f64>` of generated normals. | Deterministic for identical sampling method, dimensions, point count, and seed. | Generation-only helper for benchmarking, diagnostics, and future tool surfaces; not a full simulation execution API. Scrambled Sobol generation uses a batched four-dimensional path, and structured pricing paths now use path-level filling so Sobol pricing benefits from the same strategy. | Strong benchmark and diagnostics surface; useful for agent QMC inspection tools. |
| `structured_sampling_guidance_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Return method-specific sample-size and dimension guidance for structured sampling. | Inputs: `SamplingMethod`, point count, dimension count. Output: `StructuredSamplingGuidance` with recommended point count, power-of-two status, warnings, and notes. | Deterministic. | Guidance is rule-based; it does not replace measured estimator validation for a specific workload. | Strong agent/tool preflight surface for QMC method selection. |
| `diagnose_standard_normals_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Generate standard normals and report distribution-quality diagnostics. | Inputs: `SamplingMethod`, point count, dimension count, and seed. Output: `StandardNormalDiagnostics` with mean, variance error, tail error, axis moment errors, finite status, and warnings. | Deterministic for identical inputs. | Distribution diagnostics are sanity checks, not a proof of estimator convergence or low discrepancy. | Strong agent/tool diagnostic surface for benchmark and sampling audits. |
| `diagnose_standard_normal_samples_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Diagnose an existing row-major standard-normal sample matrix. | Inputs: sample slice and dimension count. Output: `StandardNormalDiagnostics`. | Deterministic. | Axis diagnostics assume row-major samples and are partial when sample length is not a multiple of dimensions. | Strong agent/tool diagnostic surface when samples come from another execution path. |
| `black_scholes_european_call_price` | `crates/mc-core/src/runtime/cpu.rs` | Compute the analytic Black-Scholes price for a European call under GBM assumptions. | Inputs: spot, strike, rate, volatility, and maturity. Output: analytic `f64` call price. | Deterministic. | Applies only to the vanilla European-call Black-Scholes/GBM setting; invalid parameters assert. | Strong reference helper for validation and agent explanations. |
| `black_scholes_european_call_greeks` | `crates/mc-core/src/runtime/cpu.rs` | Compute analytic Black-Scholes European-call price and Greeks. | Inputs: spot, strike, rate, volatility, and maturity. Output: `BlackScholesGreeks` with price, Delta, Vega, Rho, and conventional calendar Theta. | Deterministic. | Applies only to vanilla European calls under Black-Scholes/GBM assumptions. | Strong reference surface for Greek validation and benchmark accuracy gates. |
| `GreekReport` / `GreekEstimate` | `crates/mc-core/src/runtime/cpu.rs` | Structured sensitivity result schema for agent-readable Greek output. | Fields include workload, estimator, paths, steps, seed, base price/stderr, Greek values, optional standard errors, bump sizes, bumped prices, notes, and warnings. | Deterministic when produced by deterministic Greek functions with fixed seeds. | Timing is measured by benchmarks, not embedded by default; finite-difference standard errors are conservative propagated estimates. | Strong agent/tool result schema. |
| `european_call_greeks_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Compute European-call Greeks with explicit estimator selection. | Inputs: `&EuropeanCallConfig`, `GreekEstimator::{BumpAndRevalue, Pathwise, LikelihoodRatio}`. Output: `GreekReport`. | Deterministic for identical config, seed, and estimator. | Pathwise and likelihood-ratio support is for terminal GBM European calls; bump-and-revalue uses common random numbers. | Strong agent/tool surface for price-and-risk workflows. |
| `arithmetic_asian_call_greeks_cpu`, `down_and_out_call_greeks_cpu`, `lookback_call_greeks_cpu`, `basket_call_greeks_cpu`, `heston_european_call_greeks_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Compute workload-specific Greeks using explicit estimator selection. | Inputs: workload config and `GreekEstimator`. Output: `GreekReport`. | Deterministic for identical config, seed, and estimator. | Bump-and-revalue is currently the supported estimator for these workload families. Unsupported pathwise/LR requests return structured warnings rather than silent approximations. | Strong agent/tool surface for broad risk reporting. |
| `price_all_current_greeks_bump_and_revalue_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Run bump-and-revalue Greek coverage across all current CPU workload families. | Inputs: path count, step count, seed. Output: `Vec<GreekReport>` covering European, arithmetic Asian, down-and-out, lookback, basket, and Heston workloads. | Deterministic for identical inputs. | Breadth audit helper, not a replacement for workload-specific tuned Greek execution. | Strong benchmark and agent audit surface. |
| `AnalyticPricingComparison` | `crates/mc-core/src/runtime/cpu.rs` | Structured result object for realized-error pricing comparisons against analytic references. | Fields include workload, sampling, paths, steps, analytic price, pseudorandom and structured prices/stderrs/errors, absolute-error ratio, diagnostics, guidance, and warnings. | Deterministic when produced by deterministic comparison functions. | Current producer covers European calls against Black-Scholes only. | Strong result schema for future agent accuracy-audit tools. |
| `compare_european_call_sampling_quality_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Compare a structured sampling method against the pseudorandom European-call CPU baseline. | Inputs: `&EuropeanCallConfig` and `SamplingMethod`. Output: `PricingQualityComparison` with baseline/structured price, stderr ratio, price delta, normal diagnostics, guidance, and warnings. | Deterministic for identical config, sampling, and seed. | Compares empirical standard errors, not realized error against an analytic price; use analytic fixtures where available before making convergence claims. | Strong agent/tool surface for QMC pricing audits. |
| `compare_european_call_realized_error_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Compare structured European-call sampling against pseudorandom sampling using Black-Scholes realized error. | Inputs: `&EuropeanCallConfig` and `SamplingMethod`. Output: `AnalyticPricingComparison` with analytic price, pseudorandom and structured prices, absolute errors, stderr-unit errors, error ratio, diagnostics, guidance, and warnings. | Deterministic for identical config, sampling, and seed. | Realized error is seed- and path-count-sensitive; the analytic reference applies only to the European GBM workload. | Strong agent/tool surface for accuracy audits because it ties estimator output to a known reference. |
| `compare_arithmetic_asian_sampling_quality_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Compare a structured sampling method against the pseudorandom arithmetic-Asian CPU baseline. | Inputs: `&ArithmeticAsianCallConfig` and `SamplingMethod`. Output: `PricingQualityComparison`. | Deterministic for identical config, sampling, and seed. | No analytic arithmetic-Asian reference is attached yet; treat stderr ratios as quality signals, not proof of correctness. | Strong agent/tool surface for QMC pricing audits. |
| `compare_down_and_out_sampling_quality_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Compare a structured sampling method against the pseudorandom down-and-out CPU baseline. | Inputs: `&DownAndOutCallConfig` and `SamplingMethod`. Output: `PricingQualityComparison`. | Deterministic for identical config, sampling, and seed. | Barrier discontinuities can reduce QMC effectiveness; warnings should be surfaced to users and agents. | Strong agent/tool surface for QMC pricing audits. |
| `compare_lookback_call_sampling_quality_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Compare a structured sampling method against the pseudorandom fixed-strike lookback-call CPU baseline. | Inputs: `&LookbackCallConfig` and `SamplingMethod`. Output: `PricingQualityComparison`. | Deterministic for identical config, sampling, and seed. | The comparison uses empirical stderr ratio for a discretely monitored fixed-strike lookback payoff; no analytic lookback reference is attached yet. | Strong agent/tool surface for path-dependent QMC pricing audits. |
| `heston_european_call_price_mc_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Execute the CPU Heston stochastic-volatility European-call workload with full-truncation Euler path simulation. | Input: `&HestonEuropeanCallConfig` with spot, strike, rate, initial variance, mean-reversion, long-run variance, vol-of-vol, correlation, maturity, paths, steps, seed, thread count, and technique. Output: `HestonEuropeanCallResult`. | Deterministic for identical config, seed, thread count, and technique. | CPU reference only; discretization bias depends on step count, and the current reference check is the Black-Scholes limit rather than a general analytic Heston price. | Strong candidate for future reference execution and tool wrapping. |
| `HestonEuropeanCallPricer` | `crates/mc-core/src/runtime/cpu.rs` | Expressive builder-style API for configuring and executing the CPU Heston European-call runtime. | Inputs: chained builder methods including stochastic-volatility parameters and variance-reduction technique. Output: `HestonEuropeanCallResult` through `price()` or `HestonReferenceComparison` through `black_scholes_limit_check()`. | Deterministic for identical configuration and seed. | Focused on one Heston payoff family; native GPU execution and structured sampling are not implemented yet. | Strong ergonomic candidate for future Python and agent wrappers. |
| `compare_heston_black_scholes_limit_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Validate the Heston path runtime against the Black-Scholes limit when vol-of-vol is zero and variance is constant. | Input: `&HestonEuropeanCallConfig`. Output: `HestonReferenceComparison` with reference price, simulated price, stderr, absolute error, stderr-unit error, and warnings. | Deterministic for identical config and seed. | The reference is exact only for the zero-vol-of-vol, constant-variance limit; warnings flag non-limit configurations. | Strong agent/tool surface for correctness audits and benchmark evidence. |
| `HestonReferenceComparison` | `crates/mc-core/src/runtime/cpu.rs` | Structured result object for Heston reference validation. | Fields include reference name, paths, steps, reference price, simulated price, stderr, signed/absolute error, stderr-unit error, and warnings. | Deterministic when produced by deterministic comparison functions. | Currently produced by the Black-Scholes-limit comparison only. | Strong result schema for future agent accuracy-audit tools. |
| `gaussian_uncertainty_mean_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Execute a non-option Gaussian uncertainty-propagation benchmark with an analytic mean. | Input: `&GaussianUncertaintyConfig` with sample count, dimensions, seed, and sampling method. Output: `GaussianUncertaintyResult` with mean, stderr, analytic mean, and absolute error. | Deterministic for identical config and seed. | The response function is a focused smooth UQ benchmark, not a general UQ modeling framework. | Strong benchmark and agent-demo surface for non-option QMC behavior. |
| `european_call_price_mc_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Execute the current CPU European-call workload using the default specialized method selection. | Input: `&EuropeanCallConfig`. Output: `EuropeanCallResult`. | Deterministic for identical config and seed. | Defaults to the specialized terminal-distribution fast path unless method is made explicit elsewhere. | Good reference surface, but method choice should be explicit in tool wrappers. |
| `european_call_price_mc_cpu_with_method` | `crates/mc-core/src/runtime/cpu.rs` | Execute the CPU European-call workload with an explicit methodology choice. | Inputs: `&EuropeanCallConfig`, `EuropeanCallMethod`. Output: `EuropeanCallResult`. | Deterministic for identical config, seed, sampling, technique, and method. | Still workload-specific to European calls under GBM. | Strong candidate for a future reference execution tool. |
| `european_call_price_mc_cpu_stepwise` | `crates/mc-core/src/runtime/cpu.rs` | Run the fair step-wise CPU path that materially executes `n_steps`. | Input: `&EuropeanCallConfig`. Output: `EuropeanCallResult`. | Deterministic for identical config and seed. | General enough for fair benchmarking, but still workload-specific. | High-value benchmarking and validation tool surface. |
| `european_call_price_mc_cpu_terminal` | `crates/mc-core/src/runtime/cpu.rs` | Run the specialized terminal-distribution fast path. | Input: `&EuropeanCallConfig`. Output: `EuropeanCallResult`. | Deterministic for identical config and seed. | Specialized optimization, not a fair proxy for all path-dependent workloads. | Good specialized tool surface if labeled explicitly. |
| `EuropeanCallPricer` | `crates/mc-core/src/runtime/cpu.rs` | Expressive builder-style API for configuring and executing the current CPU European-call runtime. | Inputs: chained builder methods including path method, sampling selection, and technique selection. Output: `EuropeanCallResult` through `price()`. | Deterministic for identical configuration, method, sampling, technique, and seed. | Focused on one workload family for now. | Strong ergonomic candidate for future Python and agent wrappers. |
| `arithmetic_asian_call_price_mc_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Execute the CPU arithmetic-Asian-call workload with step-wise path simulation. | Input: `&ArithmeticAsianCallConfig`. Output: `ArithmeticAsianCallResult`. | Deterministic for identical config and seed. | Currently limited to the arithmetic Asian call under GBM. | Strong candidate for future reference execution and tool wrapping. |
| `ArithmeticAsianCallPricer` | `crates/mc-core/src/runtime/cpu.rs` | Expressive builder-style API for configuring and executing the CPU arithmetic-Asian-call runtime. | Inputs: chained builder methods including sampling and technique selection. Output: `ArithmeticAsianCallResult` through `price()`. | Deterministic for identical configuration, sampling, technique, and seed. | Focused on one workload family for now. | Strong ergonomic candidate for future Python and agent wrappers. |
| `arithmetic_asian_call_price_mlmc_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Execute the CPU arithmetic-Asian-call workload with a coupled MLMC or MLQMC estimator. | Input: `&ArithmeticAsianMlmcConfig` with base steps, level count, refinement factor, paths per level, seed, sampling method, and scramble replicate count. Output: `ArithmeticAsianMlmcResult` with total estimate, stderr, per-level means/variances, path counts, step-update cost, and replicate estimates. | Deterministic for identical config and seed; statistical reproducibility depends on level/path allocation, sampling, and replicate count. | CPU reference only; currently specialized to arithmetic Asian calls under GBM. `ScrambledSobol` enables MLQMC, and `scramble_replicates > 1` gives a replicate-based error estimate. Barrier MLMC is intentionally not exposed yet because discontinuous payoffs need separate treatment. | Strong agent/runtime surface for MLMC and MLQMC experiments. |
| `tune_arithmetic_asian_mlmc_allocation_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Estimate per-level MLMC path counts from pilot variances under a target step-update budget. | Inputs: `&ArithmeticAsianMlmcConfig`, target step updates, pilot paths per level. Output: `ArithmeticAsianMlmcAllocationPlan` with recommended paths, pilot variances, per-path costs, and estimated cost. | Deterministic for identical config and seed. | Pilot-based heuristic, not a proof of optimality; rerun when workload parameters or sampling method changes. | Strong agent/tool surface for benchmark tuning and explainable allocation. |
| `solve_arithmetic_asian_mlmc_tolerance_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Solve an arithmetic-Asian MLMC/MLQMC path allocation from pilot variances for a target standard error. | Inputs: `&ArithmeticAsianMlmcConfig` and `&ArithmeticAsianMlmcToleranceConfig`. Output: `ArithmeticAsianMlmcTolerancePlan` with estimated stderr, target-hit flag, max-budget flag, chosen paths per level, allocation details, and a ready-to-run recommended config. | Deterministic for identical config, tolerance, seed, and sampling method. | CPU reference planning only; estimated stderr comes from pilot variances and should be validated by execution/benchmarks. For replicated MLQMC, the target applies to the averaged replicate estimator. | Strong agent/tool surface for explainable accuracy-budget planning. |
| `ArithmeticAsianMlmcPricer` | `crates/mc-core/src/runtime/cpu.rs` | Builder-style API for configuring, tolerance-planning, and executing arithmetic-Asian MLMC or MLQMC. | Inputs: chained builder methods including model parameters, base steps, levels, refinement factor, paths per level, seed, sampling, and scramble replicates. Outputs: `ArithmeticAsianMlmcTolerancePlan` through `solve_tolerance()` or `ArithmeticAsianMlmcResult` through `price()`. | Deterministic for identical configuration and seed. | Focused on one workload family; tolerance planning is pilot-estimated and still needs broader workload validation. | Strong ergonomic candidate for future Python and agent wrappers. |
| `down_and_out_call_price_mc_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Execute the CPU down-and-out-call workload with step-wise path simulation. | Input: `&DownAndOutCallConfig`. Output: `DownAndOutCallResult`. | Deterministic for identical config and seed. | Current path is barrier-specific and still within the GBM family. | Strong candidate for future reference execution and tool wrapping. |
| `DownAndOutCallPricer` | `crates/mc-core/src/runtime/cpu.rs` | Expressive builder-style API for configuring and executing the CPU down-and-out-call runtime. | Inputs: chained builder methods including barrier, sampling, and technique selection. Output: `DownAndOutCallResult` through `price()`. | Deterministic for identical configuration, sampling, technique, and seed. | Focused on one workload family for now. | Strong ergonomic candidate for future Python and agent wrappers. |
| `lookback_call_price_mc_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Execute the CPU fixed-strike lookback-call workload with discrete step-wise monitoring of the running maximum. | Input: `&LookbackCallConfig`. Output: `LookbackCallResult`. | Deterministic for identical config, seed, sampling method, and technique. | CPU reference only; current payoff is a fixed-strike lookback call under GBM with maximum initialized at spot and monitored on simulation steps. | Strong candidate for future reference execution and tool wrapping. |
| `LookbackCallPricer` | `crates/mc-core/src/runtime/cpu.rs` | Expressive builder-style API for configuring and executing the CPU fixed-strike lookback-call runtime. | Inputs: chained builder methods including paths, steps, sampling, and technique selection. Output: `LookbackCallResult` through `price()`. | Deterministic for identical configuration, sampling, technique, and seed. | Focused on one path-dependent workload family for now. | Strong ergonomic candidate for future Python and agent wrappers. |
| `basket_call_price_mc_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Execute the CPU two-asset terminal basket-call workload with correlated GBM terminal sampling. | Input: `&BasketCallConfig` with two spots, strike, rate, volatilities, correlation, basket weights, maturity, paths, seed, threads, technique, and sampling method. Output: `BasketCallResult`. | Deterministic for identical config, seed, sampling method, and technique. | CPU reference only; currently a two-asset terminal basket under GBM, not a general multi-asset payoff engine. | Strong candidate for future reference execution and tool wrapping. |
| `BasketCallPricer` | `crates/mc-core/src/runtime/cpu.rs` | Expressive builder-style API for configuring and executing the CPU basket-call runtime. | Inputs: chained builder methods including two spots, weights, correlation, sampling, and technique selection. Output: `BasketCallResult` through `price()`. | Deterministic for identical configuration, sampling, technique, and seed. | Focused on one two-asset basket workload for now. | Strong ergonomic candidate for future Python and agent wrappers. |
| `compare_basket_call_sampling_quality_cpu` | `crates/mc-core/src/runtime/cpu.rs` | Compare a structured sampling method against the pseudorandom two-asset basket-call CPU baseline. | Inputs: `&BasketCallConfig` and `SamplingMethod`. Output: `PricingQualityComparison`. | Deterministic for identical config, sampling, and seed. | The current comparison uses empirical stderr ratio, not realized error against a closed-form basket reference. | Strong agent/tool surface for multi-asset QMC pricing audits. |
| `MonteCarloRng::new` / `standard_normal` | `crates/mc-core/src/runtime/cpu.rs` | Deterministic RNG used by the current CPU reference runtime. | Input: seed or method call. Output: RNG state or sample. | Deterministic. | Internal RNG choice may change as runtime broadens. | Not a primary tool surface. |

### `mc-bench`

| Surface | Path | Purpose | Inputs / Outputs | Determinism | Caveats | Agent-tool readiness |
| --- | --- | --- | --- | --- | --- | --- |
| `BenchmarkSuite` / `run_benchmarks` | `crates/mc-bench/src/harness.rs` | Select and run either the full competitiveness suite or compact smoke suite. | Input: `BenchmarkSuite::{Full, Compact}`. Output: `BenchmarkReport`. | Mostly deterministic in structure, but timing values vary by machine state. | Full is the source for performance artifacts; compact is representative and intentionally omits competitor baselines and broad workload coverage. | High-value CI and agent audit tool candidate. |
| `run_default_benchmarks` | `crates/mc-bench/src/harness.rs` | Run the default full benchmark suite and produce a structured report. | No inputs. Output: `BenchmarkReport`. | Mostly deterministic in structure, but timing values vary by machine state. | Environment-sensitive and may mark competitors unavailable. | High-value CI and agent audit tool candidate. |
| `run_compact_benchmarks` | `crates/mc-bench/src/harness.rs` | Run the compact benchmark suite used by fast local checks and benchmark-gate tests. | No inputs. Output: `BenchmarkReport`. | Mostly deterministic in structure, but timing values vary by machine state. | Not suitable for competitiveness claims because it omits competitor baselines and many breadth benchmarks. | Good fast-health agent tool surface. |
| `build_competitiveness_plan` | `crates/mc-bench/src/harness.rs` | Translate benchmark results into an improvement plan, including available NumPy, Numba, and QuantLib competitor rows. | Input: `&BenchmarkReport`. Output: markdown string. | Deterministic for same report. | Plan quality depends on benchmark coverage and currently reflects the workloads we actually measure; unavailable competitor packages remain explicit. | Good support tool for automated performance review. |

### Python package scaffold

| Surface | Path | Purpose | Inputs / Outputs | Determinism | Caveats | Agent-tool readiness |
| --- | --- | --- | --- | --- | --- | --- |
| `mc_library.EuropeanCallConfig`, `ArithmeticAsianCallConfig`, `DownAndOutCallConfig` | `python/mc_library/pricing.py` | Immutable Python dataclasses for common pricing workflows. | Inputs: model parameters, paths, steps, and seed. Outputs: typed config objects. | Deterministic data containers. | Pure-Python UX layer; Rust config parity should be maintained as bindings land. | Strong agent/tool input schema candidates. |
| `mc_library.price_european_call` | `python/mc_library/pricing.py` | Price a vanilla European call with the dependency-free Python reference helper. | Inputs: `EuropeanCallConfig` or keyword overrides. Output: `PricingResult` with price, stderr, manifest, `explain()`, and `reproduce()`. | Deterministic for same Python version, config, and seed. | Not a performance-claim surface; use Rust benchmark artifacts for timing claims. | Good user/agent quickstart surface. |
| `mc_library.price_arithmetic_asian_call` | `python/mc_library/pricing.py` | Price a path-dependent arithmetic Asian call with the Python reference helper. | Inputs: `ArithmeticAsianCallConfig` or keyword overrides. Output: `PricingResult`. | Deterministic for same Python version, config, and seed. | Pure-Python helper is for usability and reproducibility demos, not high-throughput execution. | Good user/agent quickstart surface. |
| `mc_library.price_down_and_out_call` | `python/mc_library/pricing.py` | Price a down-and-out call with explicit barrier validation. | Inputs: `DownAndOutCallConfig` or keyword overrides. Output: `PricingResult`. | Deterministic for same Python version, config, and seed. | Current helper assumes `0 < barrier < spot` and discrete monitoring. | Good user/agent quickstart surface. |
| `mc_library.price_european_call_greeks` | `python/mc_library/pricing.py` | Return Black-Scholes European-call price and Greeks with reference metadata. | Inputs: `EuropeanCallConfig` or keyword overrides. Output: `GreekReport` with base price, Greeks, manifest, `explain()`, and `reproduce()`. | Deterministic. | Closed-form European GBM reference only; broader Greek estimators live in Rust. | Strong agent/tool reference and teaching surface. |
| `mc_library.PricingResult`, `GreekReport`, `ReproductionRecipe` | `python/mc_library/pricing.py` | Structured Python result schemas for explainable and reproducible workflows. | Outputs include manifest dictionaries, warnings, `explain()`, and reproduction snippets. | Deterministic for fixed inputs. | Manifest schema is `python-ux.v1` and may gain fields. | Strong result schema candidates for future agent tools. |
| `mc_library.McConfigurationError` | `python/mc_library/pricing.py` | User-facing error type with code, message, and suggestion. | Raised on invalid pricing config. Output fields: `code`, `message`, `suggestion`. | Deterministic validation behavior. | Python layer currently covers UX helper configs, not every Rust runtime error. | Strong agent/tool failure surface. |
| `mc_library.recommend_method` | `python/mc_library/methods.py` | Python-facing method recommendation helper mirroring the Rust planner policy. | Keyword inputs: workload family, paths, steps, preference flags. Output: `MethodRecommendation` with `method_id`, sampling, technique, reasons, and caveats. | Deterministic. | Pure-Python mirror until compiled bindings land; keep policy synchronized with Rust tests. | Useful early tool wrapper surface. |
| `mc_library.load_planner_evidence` | `python/mc_library/planner_intelligence.py` | Load benchmark-backed planner evidence, measured planner accuracy, winner records, and reference fixture names. | Inputs: optional repo root and benchmark artifact path. Output: JSON-serializable `planner-evidence.v1` dict. | Deterministic for a fixed artifact. | Timing and winners are hardware-local to the artifact; rerun benchmarks on target hardware before production claims. | Ready as an agent evidence tool through `mc.planner_evidence`. |
| `mc_library.measured_winner_database` | `python/mc_library/planner_intelligence.py` | Return the measured method/backend winner database derived from benchmark artifacts. | Inputs: optional repo root and benchmark artifact path. Output: JSON-serializable winner records with workload, method, backend, artifact, and runtime. | Deterministic for a fixed artifact. | Winner rows depend on artifact breadth and available hardware. | Ready as an agent-readable planner evidence surface. |
| `mc_library.cost_frontier` | `python/mc_library/planner_intelligence.py` | Rank measured method/backend rows by runtime for a workload. | Inputs: workload plus optional repo root and benchmark artifact. Output: `planner-frontier.v1` dict with ordered frontier rows and diagnostics. | Deterministic for a fixed artifact. | Empty frontier returns structured diagnostics; rows should not be generalized across machines. | Ready as an agent cost-frontier tool through `mc.cost_frontier`. |
| `mc_library.compare_methods` | `python/mc_library/planner_intelligence.py` | Compare measured runtime and accuracy tradeoffs for a workload. | Inputs: workload plus optional artifact controls. Output: `planner-comparison.v1` dict with recommended row, alternatives, and tradeoffs. | Deterministic for a fixed artifact. | Recommendation is evidence-backed but still limited by current benchmark coverage. | Ready as an agent method-comparison tool through `mc.compare_methods`. |
| `mc_library.why_not_faster` | `python/mc_library/planner_intelligence.py` | Explain why a requested method is not the current measured recommendation. | Inputs: workload, method id, and optional artifact controls. Output: `planner-why-not.v1` dict with reasons and suggestions. | Deterministic for a fixed artifact. | Requires matching benchmark rows for best explanations; otherwise returns a no-evidence diagnostic-style explanation. | Ready as an agent explanation tool through `mc.why_not_faster`. |
| `mc_library.mlmc_error_calibration` | `python/mc_library/planner_intelligence.py` | Report estimated-vs-realized MLMC/MLQMC calibration evidence. | Inputs: workload plus optional artifact controls. Output: `planner-mlmc-calibration.v1` dict with realized stderr ratios and calibration status. | Deterministic for a fixed artifact. | Current calibration evidence is arithmetic-Asian focused; barrier/discontinuous MLMC remains future work. | Ready as an agent calibration tool through `mc.mlmc_calibration`. |
| `mc_library.run_benchmarks` | `python/mc_library/benchmarks.py` | Run the Rust benchmark binary from Python and parse structured benchmark JSON. | Inputs: repo root, release flag, and profile (`"full"` or `"compact"`). Output: tuple of `BenchmarkResult`. | Benchmark timing is environment-sensitive. | Requires local Rust toolchain and repo checkout; compact profile is for smoke checks, not full competitiveness claims. | Useful audit helper, not yet a general runtime execution API. |
| `mc_library.agent_tool_manifest` | `python/mc_library/agent.py` | Return the machine-readable list of stable agent tools. | No inputs. Output: manifest dict with tool names, schema ids, determinism, and failure modes. | Deterministic. | Describes current Python-facing tool layer, not every Rust internal surface. | Ready as an agent discovery tool. |
| `mc_library.export_json_schemas` | `python/mc_library/agent.py` | Export JSON-schema-like request and response contracts for agent tools. | No inputs. Output: dict keyed by schema id. | Deterministic. | Dependency-free schema export; not generated by Pydantic or JSON Schema libraries. | Ready as an agent schema discovery tool. |
| `mc_library.agent_validate` | `python/mc_library/agent.py` | Validate supported agent workload requests without simulation execution. | Input: JSON-serializable request dict with `workload` and optional `config`. Output: dict with `ok`, diagnostics, and `agent-run.v1` manifest. | Deterministic. | Validates the current Python tool surface, not full Rust `SimulationSpec` semantics. | Ready as an agent validation tool. |
| `mc_library.agent_recommend` | `python/mc_library/agent.py` | Recommend method, sampling, and technique through the Python planner mirror. | Input: request dict with workload/config/preferences. Output: recommendation and manifest. | Deterministic. | Heuristic policy; performance claims still require benchmark artifacts. | Ready as an agent recommendation tool. |
| `mc_library.agent_plan` | `python/mc_library/agent.py` | Produce deterministic dry-run plan metadata without executing simulation. | Input: request dict. Output: plan with backend, method, sampling, estimated memory, rejected methods, notes, and manifest. | Deterministic. | Cost estimate is intentionally low-confidence until measured planner evidence is broader. | Ready as an agent planning tool. |
| `mc_library.agent_execute` | `python/mc_library/agent.py` | Execute supported Python reference workloads with reproducibility metadata. | Input: request dict. Output: result, `agent-run.v1` manifest, and reproduction recipe. | Deterministic for same config, seed, package version, and Python version. | Python reference execution is not a throughput claim; Rust runtimes remain the performance path. | Ready as a narrow reference execution tool. |
| `mc_library.agent_compare` | `python/mc_library/agent.py` | Compare fast and accuracy-oriented method choices. | Input: request dict. Output: alternatives and manifest. | Deterministic. | Planner-policy comparison only; does not run benchmark races. | Ready as a lightweight agent comparison tool. |
| `mc_library.agent_benchmark` | `python/mc_library/agent.py` | Return benchmark command metadata by default or run the benchmark harness when `execute=true`. | Input: optional dict with profile, release, execute, repo root. Output: dry-run command or benchmark rows plus manifest. | Dry-run deterministic; executed benchmarks are environment-sensitive. | Full/release benchmark execution can be slow and machine-dependent. | Ready as an agent audit tool when callers explicitly opt into execution. |
| `mc_library.agent_reproduce` | `python/mc_library/agent.py` | Create a Python reproduction snippet from an `agent-run.v1` manifest. | Input: dict containing `manifest`. Output: reproduction recipe and manifest. | Deterministic. | Supports manifests produced by current Python agent execution tools. | Ready as an agent reproduction tool. |
| `mc_library.agent_planner_evidence`, `agent_cost_frontier`, `agent_compare_methods`, `agent_why_not_faster`, `agent_mlmc_calibration` | `python/mc_library/agent.py` | Agent-safe wrappers around planner-intelligence evidence surfaces. | Inputs: JSON-serializable dicts with workload/method/artifact selectors. Outputs: `ok`, `result`, diagnostics, and `agent-run.v1` manifest. | Deterministic for fixed artifacts. | Benchmark artifacts are hardware-local; unsupported or missing rows are explicit diagnostics. | Ready as agent tools and listed in `agent_tool_manifest()`. |

## Next Catalog Expansion

As the library grows, add catalog entries for:

- future general runtime execute APIs
